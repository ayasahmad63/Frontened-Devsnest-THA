<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>THA6</title>
    <script>
        Ans 1
        const arr1='w3resource'
        const arr2=[1,2,4,0];
        console.log(Array.isArray(arr1));
        let num1=0,num2=0;
       
        



        //Ans 2
        var arr=[1,2,4,0]
        var clonearr=arr;
        console.log(arr);
        console.log(clonearr);
      
        //Ans 3
        let n=1;
        let a1=[7,9,0,-2];
        console.log(a1.slice(0,n));
        let a2=[3];
        console.log(a2.slice(0,0));
        n=3;
        let a3=[7,9,0,-2,3];
        console.log(a3.slice(0,n));
        let a4=[7,9,0,-2,6];
        n=4;
        console.log(a4.slice(0,n));
        let a5=[7,9,0,-2,-3];
        n=0;
        console.log(a5.slice(0,n));


        //Ans 4
        myColor=["Red","Green","White","Black"];
        console.log(myColor.join(',')) ;
        console.log(myColor.join('+')) ;

        //Ans 5
        const arr1=[3,'a','a','a',2,3,'a',3,'a',2,4,9,3];
        let n=0;
        for(let i=0;i<arr1.length;i++)
        {
         if(arr1[i]==='a')
         {
             n++;
         }
        }
        console.log( n );

        arr1.forEach((item,index)=>{
         if(item=='a')
         {
             n++;
         }
      })
        
       
    </script>
</head>
<body>
    
</body>
</html>
